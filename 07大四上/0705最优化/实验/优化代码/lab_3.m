% 定义二次规划的参数
H = [1 -1; -1 1];
c = [-2; -6];
A = [1 1; -1 2; 2 1];
b = [2; 2; 3];
Aeq = []; % 无等式约束
beq = [];
VLB = [0; 0]; % 下界
VUB = []; % 无上界

% 求解二次规划问题
[x, z] = quadprog(H, c, A, b, Aeq, beq, VLB, VUB);

% 显示最优解
disp('最优解 (x1, x2):');
disp(x);
disp('最优目标函数值 Z:');
disp(z);
